<% if @rooms.empty?%>
	<b>All the rooms are authorizes</b>
<%else%>
	<ul>
	<% @rooms.each do |room|%>
		<li><%= room.name%>
			<%=room.address%>
			<%= room.price%>
		</li>
		<%= form_for room do |f|%>
		<%= f.hidden_field :is_authorized, value: "false"%>
		<%= f.submit "Unauthorize" %>
		<% end %>
	<% end %>
	</ul>
	<% end %>

