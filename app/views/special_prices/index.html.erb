<p id="notice"><%= notice %></p>

<h1>Listing Special Prices</h1>

<table class="table">
  <thead>
    <tr>
      <th>Room</th>
      <th>Start date</th>
      <th>End date</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @special_prices.each do |special_price| %>
      <tr>
        <td><%= special_price.room_id %></td>
        <td><%= special_price.start_date %></td>
        <td><%= special_price.end_date %></td>
        <td><%= special_price.price %></td>
        <td><%= link_to 'Show', special_price %></td>
        <td><%= link_to 'Edit', edit_special_price_path(special_price) %></td>
        <td><%= link_to 'Destroy', special_price, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Special price', new_special_price_path %>
